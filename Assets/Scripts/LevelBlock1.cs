using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class LevelBlock1 : MonoBehaviour
{

    public Transform startPoint, exitPoint; //Transform tipo de variable que determina puntos de coordenadas de posición

    // Start is called before the first frame update
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
